function App() {
  return (
    <main className="min-h-screen bg-[color:var(--color-bg)] text-[color:var(--color-fg)]">
      <div className="mx-auto flex min-h-screen max-w-3xl flex-col items-center justify-center gap-4 px-6 text-center">
        <h1 className="font-display text-4xl font-bold tracking-tight">
          Markdown Lens
        </h1>
        <p className="max-w-md text-[color:var(--color-fg-muted)]">
          Scaffold is live. The renderer arrives in the next phase.
        </p>
      </div>
    </main>
  )
}

export default App
